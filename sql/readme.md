These files are history and reasoning, applied in numeric order 01→10. They are not the current state of the database: DB_SCHEMA_CURRENT.csv in the Claude project is, generated by 00_schema_snapshot.sql. Where a file and the snapshot disagree, the snapshot is right. Read these for why a rule exists, never for whether it is live.
